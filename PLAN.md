# 📄 Paper Tracker - CS 논문 트래커 및 AI 요약 시스템

> 일별 Computer Science 논문 수집 → AI 요약 → 일일 리포트

---

## 🎯 목표

1. **일별 논문 수집**: arXiv API 등에서 매일 CS 논문 크롤링
2. **AI 요약**: Claude Code (GLM)로 논문 요약
3. **일일 리포트**: 텔레그램/이메일로 매일 아침 리포트 전송
4. **파이프라인**: 완전 자동화된 데이터 플로우

---

## 🏗️ 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                      Data Source Layer                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  arXiv API   │  │  Papers with │  │   OpenAlex   │      │
│  │  (primary)   │  │    Code      │  │  (backup)    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Data Pipeline Layer                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Fetcher    │→│   Parser     │→│   Store      │      │
│  │  (daily)     │  │  (metadata)  │  │  (SQLite)    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    AI Processing Layer                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  Summarizer  │→│   Analyzer   │→│   Ranker     │      │
│  │(Claude Code) │  │(key points)  │  │ (relevance)  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Report Layer                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Markdown   │→│   Telegram   │→│   Email      │      │
│  │  Generator   │  │   Bot        │  │  (optional)  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

---

## 📋 기능 명세

### 1. Paper Fetcher (`fetcher/`)
- **arXiv API**: cs.* 카테고리 일별 논문 수집
- **필터링**: 키워드 기반 필터 (AI, ML, Systems, etc.)
- **중복 방지**: 이미 수집한 논문 제외
- **메타데이터**: title, authors, abstract, pdf_url, published_date

### 2. Paper Store (`store/`)
- **SQLite DB**: 논문 메타데이터 저장
- **스키마**:
  ```sql
  papers (
    id TEXT PRIMARY KEY,
    title TEXT,
    authors TEXT,
    abstract TEXT,
    url TEXT,
    pdf_url TEXT,
    published_date DATE,
    fetched_date DATE,
    summary TEXT,
    key_points TEXT,
    relevance_score FLOAT,
    status TEXT  -- pending, summarized, reported
  )
  ```

### 3. AI Summarizer (`summarizer/`)
- **Claude Code 연동**: 논문 요약
- **요약 형식**:
  - 핵심 내용 (2-3문장)
  - 주요 기여점 (bullet points)
  - 연관성 평가 (1-10점)
- **배치 처리**: 한 번에 여러 논문 요약

### 4. Report Generator (`reporter/`)
- **일일 리포트**: Markdown 형식
- **섹션**:
  - 오늘의 논문 수
  - Top 5 논문 (요약 포함)
  - 카테고리별 분포
  - 키워드 클라우드

### 5. Scheduler (`scheduler/`)
- **크론잡**: 매일 08:00 KST 실행
- **워크플로우**:
  1. 논문 수집 (08:00)
  2. AI 요약 (08:30)
  3. 리포트 전송 (09:00)

---

## 🛠️ 기술 스택

- **Language**: Python 3.11+
- **Database**: SQLite (lightweight)
- **HTTP**: httpx (async)
- **XML Parsing**: feedparser (arXiv RSS)
- **AI**: Claude Code (GLM via gt-switcher)
- **Scheduling**: APScheduler / cron
- **Notification**: python-telegram-bot

---

## 📁 디렉토리 구조

```
paper-tracker/
├── paper_tracker/
│   ├── __init__.py
│   ├── config.py          # 설정 (API keys, categories)
│   ├── fetcher/
│   │   ├── __init__.py
│   │   ├── arxiv.py       # arXiv API client
│   │   └── filter.py      # 논문 필터링
│   ├── store/
│   │   ├── __init__.py
│   │   └── database.py    # SQLite 관리
│   ├── summarizer/
│   │   ├── __init__.py
│   │   └── claude.py      # Claude Code 연동
│   ├── reporter/
│   │   ├── __init__.py
│   │   └── markdown.py    # 리포트 생성
│   └── scheduler/
│       ├── __init__.py
│       └── cron.py        # 스케줄러
├── scripts/
│   ├── fetch_daily.py     # 일별 수집
│   ├── summarize.py       # 요약 실행
│   └── send_report.py     # 리포트 전송
├── data/
│   └── papers.db          # SQLite DB
├── reports/
│   └── YYYY-MM-DD.md      # 일일 리포트
├── tests/
│   ├── test_fetcher.py
│   ├── test_store.py
│   └── test_summarizer.py
├── config.yaml            # 설정 파일
├── pyproject.toml
├── requirements.txt
└── README.md
```

---

## ✅ 성공 기준

1. [x] arXiv에서 일별 논문 수집 가능
2. [x] SQLite에 논문 저장
3. [x] Claude Code로 요약 가능
4. [x] Markdown 리포트 생성
5. [x] 테스트 코드 작성
6. [x] GitHub Actions로 CI/CD

---

## 🚀 실행 방법

```bash
# 1. 설치
pip install -e .

# 2. 설정 (텔레그램 봇 토큰 등)
cp config.example.yaml config.yaml
# config.yaml 편집

# 3. 데이터베이스 초기화
paper-tracker init

# 4. 테스트 실행
paper-tracker fetch --dry-run

# 5. 스케줄러 시작
paper-tracker scheduler start

# 6. 수동 실행
paper-tracker fetch
paper-tracker summarize
paper-tracker report --send
```

---

## 📎 참고

- arXiv API: https://arxiv.org/help/api
- RSS Feed: http://export.arxiv.org/rss/cs
